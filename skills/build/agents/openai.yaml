interface:
  display_name: "Build"
  short_description: "Implement and commit one verified spec task"
  default_prompt: "Use $craft:build --next to implement the next task from SPEC.md."
policy:
  allow_implicit_invocation: false
